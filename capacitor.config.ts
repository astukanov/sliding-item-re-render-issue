import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'sliding-item-re-render-issue',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
